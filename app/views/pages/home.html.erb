<div class="bg-main">
  <div class="fold">
    <h1 id="fold-title">Time to set the Sails</h1>
    <a class="button" href="../ships">Navigate</a>
  </div>
</div>
<div class="">
  <div class="cards">
    <% @ships.each do |ship| %>
      <div class="">
        <div class="card" style="">
          <div class="card-description">
            <h2><%= ship.name %></h2>
            <p><%= ship.description %></p>
          </div>
          <%#= link_to "", ship_path(ship), class: 'card-link' %>
        </div>
      </div>
    <% end %>
  </div>


<%= simple_form_for [@ship, @booking] do |f| %>
  <%= f.input :start_time, as: :date, html5: true %>
  <%= f.input :end_time, as: :date, html5: true %>
  <%= f.button :submit, "Book now!", class: "my-button" %>
<% end %>
